[package]
name = "gaia-acp"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "GAIA 2.0 local-first agent control plane — fake MCP gateway, policy, approval, sandbox, audit (#341–#348)"

[dependencies]
hex = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
