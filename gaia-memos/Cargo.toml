[package]
name = "gaia-memos"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "GAIA 2.0 Memory OS (MemOS + Continuity)"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
