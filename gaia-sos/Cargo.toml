[package]
name = "gaia-sos"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Super OS design contracts first cut. No second kernel. Not v1.0."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
gaian = []
