[package]
name = "gaia-earth"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Artificial Twin of Earth — observation contract. Not a live planet model."

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]
