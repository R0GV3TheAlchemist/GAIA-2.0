[package]
name = "gaia-interface"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "L6 sovereign interface — local session, CLI/HTTP adapters, revoke"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
