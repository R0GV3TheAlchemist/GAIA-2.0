# GAIA Symbolic Coherence — listed bind

**Status:** listed. Not a scoring runtime.  
**Issue:** #772  
**Related:** #766 #767 #770 #771

This file satisfies the first acceptance item of #772 and preserves the S4
record as documentation. It does **not** add `CoherenceScore`, a
`meaning_density` metric in GAIAN, or a naming linter.

## Premise (honest)

A provenance-signed, policy-gated system can still fail to *matter*.
Meaning, symbol, and care are design constraints on language and refusal —
not a second kernel and not a simulated heart.

Frankl, Jung, and HeartMath citations in #772 are **sources for the epic**.
They are not imported as crates. Heart-field amplitudes are not GAIA APIs.

## Four listed actions

### S1 — Symbolic coherence in names
Existing names (`GAIA`, `GAIAN`, `gaia-aikd`, `gaia-kernel`, `gaia-geometry`)
are already on `main`. New module names SHOULD be checked for technical
clarity first, archetypal resonance second. No rename sweep in this PR.

### S2 — Meaning density
A future GAIAN rubric MAY add `meaning_density` next to citation coverage and
uncertainty honesty. Not implemented here. Optimising engagement or dependence
is refused.

### S3 — Coherence as honesty, not electromagnetism
#772 sketches `coherence_check`. **Refuse to add the type now.** The four
components it names already exist as tests and flags:

| Sketch component | Existing surface |
|---|---|
| uncertainty expressed | `gaia-aikd` / `gaia-gaian` honesty tests |
| real citations | AIKD cite-or-verify posture |
| care constraints | ACP obligations / GAIAN consent tests |
| no false certainty | honesty flags that stay `false` until earned |

### S4 — Record of 21–22 Sep 2026
Issue #772 records that the owner recognized the care constraints as
*discovered*, not bolted on. That moment is a project-history note. It is
not a milestone tag, not a release, and not evidence of sentience.

## Refuse

- No `CoherenceScore` struct.
- No sentient twin, no heart-EM field sensor, no planetary telekinesis.
- Do not claim the name “GAIA” makes the runtime the Great Mother archetype.
