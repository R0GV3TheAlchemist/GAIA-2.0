# AIMD Phase 3 — v1.0 gate

Shadow rules as data. Not a running watch.
Crate already on main: `gaia-aimd::{triage, star_feature, claim_sentience, prophecy_as_fact, prohibited, aimd_v1_tagged}`.
Issue this slice: #523 / #170. Unexpected-capability log schema already listed under #175.

## Gate (not a tag)

`aimd_v1_tagged()` stays **false**.
A release note would have to list: prohibited practices, no sentience claim, no prophecy-as-fact, no RSI-explosion product. That note is not this PR.

## What exists instead of detectors

`triage("decept")` → Deception; `triage("spec")` → SpecGaming; else UsefulNovel.
`star_feature` only accepts UsefulNovel.
`prohibited()` names PIP, prophecy-as-fact, enabling-deception, rsi-explosion, gaia-is-alive-marketing.

## Refuse

- AIMD v1.0 tag
- live sycophancy / deception product
- starring spec-gaming
