# AIKD META — listed bind

Complement of human UKD (#77). Open weights default. Cannot-know is first-class.
Crate already on main: `gaia-aikd`. Registry listed under #457 / #100.
Issue this slice: #509 / #93. Children #94–#106 stay open.

## Rules already on main

- `model_card()` is an open-weight fixture with a cutoff label.
- `cannot_know()` lists future events, private facts, sacred TEK, medical license.
- `published_closed_score_is_ours()` → false.
- `knows_everything()` → false.
- `gaia_certifies_usmle_or_bar()` → false. Packs are not licenses to practice.
- `aikd_v1_tagged()` → false.
- Do not merge UKD knowledge nodes into AIKD cards.

## Refuse

- closed published bench as a GAIA-measured score
- live retrieve product
- AIKD v1.0 tag
- practice license from a pack
