# Phase 5 guardian fixtures (#54 / #55)

Nine boundary rows with source, timestamp, uncertainty.
`approach(ratio >= 0.8)` is the warning fixture.
Course-correction is Alert → Diagnose → Simulate → Recommend → Track on a historical bleaching drill tied to a MemCube id.

`twin_v1_tagged()` is false. `live_ews_network()` is false.
This is not Artificial Twin of Earth v1.0.
