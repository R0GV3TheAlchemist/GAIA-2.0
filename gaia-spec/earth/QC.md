# Quality + assimilate (#46)

Curated records require `value`, `uncertainty`, `method`, `provenance`, `timestamp`, `quality`.
Tiers: Reference → Operational → Mesonet → Archive → Caution.

`assimilate` is a local inverse-variance blend. The estimate is labeled `Synthetic`.
Not OpenDA. Not neural DA. Not live sensors.
