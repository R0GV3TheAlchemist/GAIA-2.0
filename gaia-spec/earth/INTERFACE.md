# Human-Earth interface (#38 / #52 / #53)

Same `PlaceState` via dashboard, scientist API, and locale `ask` (`EarthInterface`).
Demo globe layers: Live, Forecast, Scenario. Not Cesium.
Contribution pin requires license + attribution.
Scientist API and Policy UI share `ScenarioLibrary` ids.
Local agent returns source + uncertainty and refuses weapon purposes.
`surveillance_endpoints()` is empty. Basic access is not paywalled.
