# Mission catalog (#44)

Machine list is `gaia-earth::MissionCatalog::seed` and `missions.csv`.
`tick(now)` updates one product per domain (space, ocean in-situ, seismic). Not a scheduled harvest.

Gaps named: deep ocean, poles, low-income regions.
