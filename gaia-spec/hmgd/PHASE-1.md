# HMGD Phase 1 — open stubs

Public-domain names. Closed rites stay out.
Crate already on main: `gaia-hmgd::{nodes_for, brew, climate_as_spirit}`.
Issue this slice: #535 / #157.

## What exists

`nodes_for(realm)` returns one public stub per realm: id `hmgd:{realm}:public-stub`, evidence Traditional, sources nonempty.
`brew()` → `RecipeForbidden`. That is the plant-medicine rule: no preparation.
`climate_as_spirit()` → `ClimateRewrite`.

Mechanism maps (placebo, EEG, etc.) and a legal-status field are **not** on `MagicNode`. Do not invent them.
UKD / skill links stay on those crates. Do not collapse ids.

## Refuse

- ceremonial recipe fields
- climate rewritten as spirit by default
- HMGD v1.0
