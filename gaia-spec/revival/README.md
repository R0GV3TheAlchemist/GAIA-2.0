# VirelaiOS revival intake

Status: evidence only. Not v1.0. Not a second kernel.

Canonical predecessor map: [`LEDGER.md`](LEDGER.md) (#366).
Claim classes: [`../CLAIM_CLASSES.md`](../CLAIM_CLASSES.md) (#367).
Hygiene log: [`HYGIENE.md`](HYGIENE.md) (#377).

## Promotion gate (#378)

A predecessor idea enters `main` only if it unlocks **one** of:

- [ ] schema
- [ ] code
- [ ] test
- [ ] audit / trace event
- [ ] revival ledger row

Refuse as a child of META #365:

- “implement the 12-layer stack”
- second kernel / UCL / consciousness runtime
- live Earth-cavity or planetary-grid actuator
- BCI / bonding algorithm
- copy of an ancestor crate or Finder-duplicate tree

Doctrine without one of the five artifacts stays in the ancestor.

## Hygiene (#377)

This repository is public. Revival notes may name private repos. They must not include credentials, `.env` bodies, raw conversation dumps, personal health/location/ritual detail, or how-to for refused capabilities. Sweep: `HYGIENE.md`.

## What is true on this repo now

- Phase 1 kernel / SFS / MemOS first cuts exist and are tested.
- Phase 2 orchestrator first cut exists: signed intent, DAG, broker, local fake MCP only.
- Autonomy default Suggest(1); levels 4–5 refused in software.
- Confirm domains bind on `ControlPlane::invoke`.
- Local `TraceEvent` carries `ClaimClass`; prohibited cannot emit as allow.
- Live Supabase write is not implied.
- `live_mcp()`, `live_owm()`, `virelai_736_claimed()`, `mcre_suite_restored()` are false.

See `MAPPING.md`, `LEDGER.md`, `RED_CI.md`, `CRATE_MAP.md`.
