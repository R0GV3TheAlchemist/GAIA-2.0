# Private predecessor: R0GV3TheAlchemist/NEXUS-Old-Repository

Private. Python-labeled. `main` @ `59ea6fd`. Last update 2026-08-22. Zero open issues.

README is already the right cut: Schumann / 7.83 Hz is a **configured constant check**, not a live Earth-cavity measurement. `.gaia-ci-note` says circular-import fix in progress.

Root is a large canon/spec surface (`ARCHITECTURE.md`, `CANON_BRIDGE.md`, `COEXISTENCE_LAWS.md`, `DISCERNMENT_PROTOCOL.md`, digital-twin specs, Dockerfile). Same family as the NEXUS zip we refused to bulk-import.

## Red does not mean “unused”

Fixing CI in an ancestor repo does **not** authorize merging that repo into GAIA-2.0.

| Bucket | If we make it green | Still usable in 2.0? |
| --- | --- | --- |
| Compile / import / test failures | Yes, as a *local* repair of that ancestor | Only the contracts that match 2.0 (deny, confirm, audit, twins as *records*) |
| Doctrine / coexistence / discernment | Tests were never the point | As values and refusal language |
| Planetary CI, energy-grid interface, live EM resonance, dimensional OS | Green tests would still over-claim | No |

Using *all* of the red trees would undo the autonomy cap we just merged (#362) and recreate Virelai R-011 (specification creep).

Keep ancestors as sources. Promote one contract at a time into `gaia-acp` / `gaia-spec`.
