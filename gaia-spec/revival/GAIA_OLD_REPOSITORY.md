# Private predecessor: R0GV3TheAlchemist/GAIA-Old-Repository

Private. `main` @ `72071153`. Last update 2026-08-22. GitHub language label is JavaScript because `apps 2/desktop 2/node_modules` was committed. That is a Finder-duplicate dump, not the product.

## What it actually is

The first **implementation trunk** after the knowledge-plane repo. README is honest: scaffold, not a finished OS.

Rust workspace (`Cargo.toml`):

- `gaia-contracts`, `gaia-core`, `gaia-crypto`, `gaia-policy`
- `gaia-vault`, `gaia-memory`, `gaia-session`, `gaia-server`
- `gaia-fs`, `gaia-notify`, `gaia-power`, `gaia-net`, `gaia-scheduler`, `gaia-ffi`

Also: JSON schemas, claim-validation / release-integrity / policy-audit workflows, Tauri+React desktop skeleton, Python services package.

`GAIAmanifest.json` itself defers quantum accelerators, resonance crystal matrix, and sentience lanes. Release gates are schema + `cargo test` + pytest + audit on every event + logged permission denial.

## Lift (contracts, not trees)

1. Doctrine → schema → code → tests → audit → release as the only promotion path.
2. Claim-validation CI (established vs experimental vs symbolic vs prohibited).
3. Vault / memory / policy crate *names* as a map onto current `gaia-acp` — do not copy the old crates wholesale; 2.0 already has policy, audit chain, autonomy.
4. Local-first + permission denial must be logged (already true in 2.0).

## Do not lift

- Folders named `apps 2`, `.github 2`, `.cargo 2`, or any `node_modules`.
- Quantum / Orch-OR / sentience / 12-dimension / resonance-crystal specs as runtime.
- Body Matrix / Crystal Matrix as physics law.
- A second desktop shell next to `gaia-interface`.

This repo is the ancestor of the *crate layout*. GAIA-2.0 is the living enforcement plane.
