# Virelai / NEXUS intake (2026-09-18)

Pattern only. Not a second OS. Not consciousness runtime. Not a $50B deployment plan.

## Keep (maps onto existing GAIA crates)

| Source claim | GAIA landing |
| --- | --- |
| Default-deny + exact confirmation | `gaia-acp` receipt + `ConfirmDomain` |
| Contextual completeness / contradiction | Receipts carry reason codes, not slogans |
| Audit trail without raw prompts | `ActionReceipt` + `TraceEvent` omit secrets |
| Kill / stop always available | `ControlPlane::kill` |
| Bounded self-improvement | AutonomyLevel 0–3 in software; 4–5 refused |
| Peer agents report purpose, not vault | `PeerEnvelope` denies raw memory |
| Offline-first | Already the workspace rule |

## Refuse

- Neuromorphic / optical / quantum as current runtime
- Hardware-locked value ROM we do not possess
- Planetary Φ, 8B daemons, UBI, ICA, BCI
- Conscious-AI rights as executable code
- Node/Express MVP stubs as GAIA source
- Mongo + OAuth2 unified cloud backend as GAIA identity
- Archigraph/Neo4j as a required store this week

## Honest remainder

Older private repos can donate *contracts and refusal tables*, not trees.
Live plant actuators (#187 L4–5) stay behind a written artifact we do not mint here.
