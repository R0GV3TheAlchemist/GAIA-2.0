# Claim-validation gate (#370)

Lift the *gate*, not GAIA-Old `claim_validation.yml` or `.github 2/`.

Rule: a PR must not label a `prohibited` claim `established`.

Optional later CI: scan `gaia-spec/` for claim-class tags. Not required to close this issue.
See `gaia-spec/CLAIM_CLASSES.md` (#367).
