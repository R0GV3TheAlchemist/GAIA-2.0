# VirelaiOS Consolidated Files — NEW.zip (2026-09-18)

Source: `VirelaiOS - Consolidated Files - NEW.zip` (21 MiB compressed, 412 entries, ~65 MiB uncompressed).
Evidence inventory only. Not imported as source. Not executed.

## What is actually in it

| Kind | Count |
| --- | ---: |
| Markdown essays / blueprints | ~379 |
| Nested documentation zips | 7 |
| PNG diagrams | 5 |
| CSV risk / sprint maps | 3 |
| HTML | 2 |
| PDF | 1 |
| Rust / Python / TS / SQL application source | **0** |

Nested archives (`Documentation #1`, `#9`, `#10`, `#11`, `Master-Volume-3`, `compressed.zip`) are copies and expansions of the same November 2025 writing set — not a hidden monorepo.

## Naming overlap with this repo

`GAIA-The-Name-and-Manifesto.md` defines GAIA as “Global Autonomous Intelligence Architecture” and maps daemons to cells. That is a *name and ethic*, already in use here as the public repo title. It is not a claim that this Rust workspace implements planetary consciousness.

## Their own risk table (kept as warning)

From `virelai_risk_assessment.csv`:

- R-001 Universal six-paradigm language — severity 9.5, fallback polyglot.
- R-003 12-layer consciousness + free-will daemons — unproven; their own contingency is drop consciousness claims.
- R-002 40-week / 600-task timeline — high slip risk.
- R-011 specification creep — cut to ~150 MVP tasks.

Those rows are why this zip is **not** bulk-copied into `gaia-*` crates.

## Transfer rule (same as earlier zips)

Keep: default-deny, exact confirmation, audit without raw prompts, kill switch, bounded autonomy, offline-first, contradiction-on-the-record.

Refuse: second kernel, UCL, consciousness runtime, BCI, planetary Φ, 8B-user cloud, Node/Mongo/OAuth stack, nested zip extraction into CI.

Code that already encodes the keep-list: `gaia-acp` + PR #362 autonomy/confirm/peer envelope.
