# VirelaiOS — Already Consolidated Files (2026-09-18)

Source zip: 62 MiB compressed, **1400 files**, ~191 MiB uncompressed.
Evidence only. Not imported. Not executed.

## Counts

| Kind | Count |
| --- | ---: |
| Markdown | 1339 |
| Unique markdown titles | ~1040 |
| PDF | 14 |
| CSV | 11 |
| Nested zips | 2 (Saela) + compressed/ |
| Files named `.py` | 3 |

## The `.py` files are essays

`METAPHYSICS-ENCODED-EXECUTABLE-OS.py`, `VIRELAILOS-COMPLETE-CODE-SYNTHESIS.py`, and `VIRELAILOS-OMNISCIENT-GOD-LEGACY (1).py` start as markdown (`# HEADING`) with fenced `class OmniConsciousness` sketches. They are not a runnable OS. They are not copied into `gaia-*`.

## Saela nested zips

- `Saela - Configuration lock proposal.zip` — two markdown blobs (~2.0 MiB + 1.9 MiB).
- `Saela - Research branch continuation.zip` — five markdown blobs (~6 MiB total).

Treat Saela as a **named lock/research stream**, not a second kernel. If a later private repo is Saela itself, inventory that repo the same way: contracts first.

## Rule (unchanged)

Keep: default-deny, confirmation domains, kill, mapped-vs-listed ledgers, local-first.
Refuse: metaphysics-as-OS, omni-consciousness classes, planetary grid as runtime, bulk 1400-file import.
