# Crystal catalog (evoke only)

Rows are names. They are not capabilities.
`claim_class` is `listed-only`. `effects` is always empty.
A row must never grant tools, memory, Autonomy 4–5, medical protocol, or network.

**Required law before more rows:** `SPECTRUM.md`
(prism + crystal system + visible sliver). Shape is optics, not a grant.

Human-safety: chemistry notes are warnings, not work instructions.
Do not ingest. Do not treat adamite or amazonite as toys.

Source rule: evoke the law of the name; do not take folklore as authority.
