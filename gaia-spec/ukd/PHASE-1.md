# UKD Phase 1 — path fixture

Ordered prerequisites. Not a path engine.
Crate already on main: `gaia-ukd::{path_to_quantum_computing, subjects, indigenous_parallel, UN_LANGS}`.
Issue this slice: #547 / #79.

## What exists

`path_to_quantum_computing()` returns three fixture steps (linear-algebra → probability → quantum-computing) with open resource *names* and difficulty. That is the demo. Not NetworkX.
`subjects()` one stub per realm.
`indigenous_parallel()` → `TekSealed`.
`UN_LANGS` is the language-name list already on main.

Bloom tags and live OCW ingest are not APIs here.

## Refuse

- live path product
- TEK parallel without grant
- UKD v1.0
