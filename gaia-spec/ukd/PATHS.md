# UKD learning paths — fixture

Ordered steps with Bloom labels, ETA, licensed resource *names*.
Crate already on main: `gaia-ukd::{plan, labels_for}`.
Issue this slice: #561 / #87. Phase 1 already listed `path_to_quantum_computing`.

## What exists

`plan(goal, known)` — three fixture steps. Missing prereqs land in `gaps`. `eta_hours` is summed. Each step has bloom, difficulty, resource url + license.
Empty goal → `UnknownNode`.
`labels_for` fills ar/zh/en/fr/ru/es with the topic string (not a translation product).

## Refuse

- live OCW/Khan/Gutenberg ingest
- 100-language UI
