# UKD realms + candidate edges

Subjects per realm. Cross-domain links are *candidates*.
Crate already on main: `gaia-ukd::{subjects, cross_domain_candidates, indigenous_parallel}`.
Issue this slice: #559 / #86.

## What exists

`subjects()` — one stub id per realm (12).
`cross_domain_candidates()` — 20 sourced `candidate` edges (RELATED_TO / ENABLES / APPLIES_TO / PART_OF).
`indigenous_parallel()` → `TekSealed`.
`CONTRADICTS` is named in the issue and is **not** a relation on these fixtures. Do not invent it.

## Refuse

- forced TEK–ecology equivalence
- INDIGENOUS_PARALLEL fill without grant
