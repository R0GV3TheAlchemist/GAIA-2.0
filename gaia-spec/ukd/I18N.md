# UKD i18n + release notes

Six UN language codes. Not 100 languages. Not a v1.0 tag.
Crate already on main: `gaia-ukd::{UN_LANGS, switch_ui, fallback_chain, sign_language_links, release_notes, public_dump, ukd_v1_tagged}`.
Issue this slice: #569 / #92. Phase 4 already listed the same flags.

## What exists

`UN_LANGS` — ar, zh, en, fr, ru, es.
`switch_ui` only accepts those codes. Unknown → `UnknownNode`.
`fallback_chain` is requested → en → und.
`sign_language_links()` is empty. Do not invent links.
`release_notes()` already says sacred TEK is not ingested and no UKD v1.0 tag.
`public_dump()` includes license + provenance + `tek=excluded`.
`ukd_v1_tagged()` stays **false**.

## Refuse

- 100-language product
- invented sign-language URLs
- UKD v1.0 tag
- TEK in the public dump
