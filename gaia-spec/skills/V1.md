# Skills v1.0 gate

Matching uses published cards only. Traditional skills use #88 grants.
Crate already on main: `gaia-skills::{SkillCard, tek_skill, skills_v1_tagged, global_profile_dump}`.
Issue this slice: #501 / #120.

## What a release note must say (not a tag)

- Sources: listed overlays + local vault. Not a live ESCO dump.
- What is not assessed: child EI, ambient sessions, clinical practice.
- Child-safety: `rank_in_marketplace` forbids age &lt; 18.
- `skills_v1_tagged()` stays **false**. This is the gate, not the release.

## Cards + TEK

`SkillCard::search` hides unpublished profiles.
`tek_skill(false)` → `NoGrant` (absent until granted).
`global_profile_dump()` stays false.

Cultural variation is named in the issue. It is **not** a `SkillCard` field on main. Do not invent it.

## Refuse

- Skills v1.0 tag
- TEK scrape / no-grant export
- ranking minors
- global profile dump
