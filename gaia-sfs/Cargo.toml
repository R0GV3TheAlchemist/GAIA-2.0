[package]
name = "gaia-sfs"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "GAIA 2.0 Semantic File System v0.1 (directory-backed + provenance + vectors)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
thiserror = "1"
